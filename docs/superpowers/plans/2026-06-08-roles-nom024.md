# Roles NOM-024 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar 4 roles diferenciados (admin, asistente_medico, cosmetista, asistente_general) con permisos granulares para cumplir la sección 2 de NOM-024-SSA3-2012.

**Architecture:** Extender el campo `rol` en la tabla `usuarios` con los 4 nuevos valores, crear middleware `requireRol(...roles)` reutilizable en backend, y aplicar condicionales de rol en frontend basados en `localStorage.getItem('rol')`. El formulario HC recibe prop `editableSections` para restringir qué secciones puede editar cada rol.

**Tech Stack:** Node.js + Express 5, PostgreSQL 16 (Docker), React 19 + Vite, JWT 8h, bcryptjs

**Spec:** `docs/superpowers/specs/2026-06-08-roles-nom024-design.md`

---

## File Map

**Create:**
- `backend/src/db/migrations/017_roles_nom024.sql`
- `backend/src/middleware/roles.js`
- `backend/src/controllers/usuariosController.js`
- `backend/src/routes/usuarios.js`
- `frontend/src/api/usuarios.js`
- `frontend/src/components/usuarios/UsuarioModal.jsx`

**Modify:**
- `backend/src/controllers/historiasClinicasController.js`
- `backend/src/controllers/notasVisitaController.js`
- `backend/src/routes/historias-clinicas.js`
- `backend/src/routes/notas-visita.js`
- `backend/src/routes/pacientes.js`
- `backend/src/routes/citas.js`
- `backend/src/index.js`
- `frontend/src/components/HistoriaClinicaForm.jsx`
- `frontend/src/components/NotaVisitaModal.jsx`
- `frontend/src/pages/PacienteDetallePage.jsx`
- `frontend/src/pages/EmpleadosPage.jsx`
- `frontend/src/App.jsx`

---

## Task 1: Migración DB 017 — roles + cédula + tipo nota

**Files:**
- Create: `backend/src/db/migrations/017_roles_nom024.sql`

- [ ] **Step 1: Crear el archivo de migración**

```sql
-- backend/src/db/migrations/017_roles_nom024.sql
-- Migración 017: Roles NOM-024, cédula profesional y tipo de nota

-- 1. Actualizar constraint de roles (primero eliminar el existente)
ALTER TABLE usuarios DROP CONSTRAINT IF EXISTS usuarios_rol_check;
ALTER TABLE usuarios ADD CONSTRAINT usuarios_rol_check
  CHECK (rol IN ('admin', 'asistente_medico', 'cosmetista', 'asistente_general'));

-- 2. Cédula profesional del usuario (requerida NOM-024 para médicos)
ALTER TABLE usuarios ADD COLUMN IF NOT EXISTS cedula_profesional VARCHAR(20);

-- 3. Migrar valor 'asistente' existente a 'asistente_medico'
UPDATE usuarios SET rol = 'asistente_medico' WHERE rol = 'asistente';

-- 4. Tipo de nota de visita
ALTER TABLE notas_visita ADD COLUMN IF NOT EXISTS tipo VARCHAR(20) NOT NULL DEFAULT 'medico';
ALTER TABLE notas_visita ADD CONSTRAINT notas_visita_tipo_check
  CHECK (tipo IN ('medico', 'cosmetico'));
```

- [ ] **Step 2: Verificar que la migración corre**

```bash
cd ~/elys/backend && node -e "require('./src/db/migrate').then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); })"
```

Esperado: `Migración aplicada: 017_roles_nom024.sql` y proceso termina con código 0.

- [ ] **Step 3: Verificar en DB que el constraint y columnas existen**

```bash
cd ~/elys && docker compose exec db psql -U elys_user -d elys -c "\d usuarios" | grep -E "rol|cedula"
cd ~/elys && docker compose exec db psql -U elys_user -d elys -c "\d notas_visita" | grep tipo
```

Esperado: columna `cedula_profesional` visible, constraint `usuarios_rol_check` actualizado, columna `tipo` en `notas_visita`.

- [ ] **Step 4: Commit**

```bash
git add backend/src/db/migrations/017_roles_nom024.sql
git commit -m "feat: migración 017 — roles NOM-024, cédula profesional, tipo nota"
```

---

## Task 2: Backend — middleware requireRol

**Files:**
- Create: `backend/src/middleware/roles.js`

**Context:** El middleware `auth.js` ya verifica JWT y pone `req.user = { id, email, nombre, rol }`. Este nuevo middleware usa ese `req.user.rol` para verificar permisos. El patrón `soloAdmin` inline en las rutas existentes NO se toca en esta tarea — se migra en Task 4.

- [ ] **Step 1: Crear el archivo middleware**

```js
// backend/src/middleware/roles.js
const requireRol = (...roles) => (req, res, next) => {
  if (!req.user || !roles.includes(req.user.rol))
    return res.status(403).json({ error: 'Sin permisos' });
  next();
};

module.exports = { requireRol };
```

- [ ] **Step 2: Verificar que el módulo carga sin errores**

```bash
cd ~/elys/backend && node -e "const { requireRol } = require('./src/middleware/roles'); console.log(typeof requireRol);"
```

Esperado: `function`

- [ ] **Step 3: Commit**

```bash
git add backend/src/middleware/roles.js
git commit -m "feat: middleware requireRol para control de acceso por rol"
```

---

## Task 3: Backend — filtro de campos HC por rol

**Files:**
- Modify: `backend/src/controllers/historiasClinicasController.js`

**Context:** El controller actual llama `HistoriaClinica.upsert(pacienteId, req.body, req.user.id)`. Para `asistente_medico` solo puede modificar campos de las secciones 6 (motivo de consulta) y 8 (exploración física). El admin puede modificar cualquier campo. Otros roles no pueden modificar HC (se protege a nivel de ruta en Task 4).

Los campos de sección 6 (mc_*) y sección 8 (sv_*, habitus, medidas, etc.) son los únicos que `asistente_medico` puede enviar; el resto se filtra silenciosamente.

- [ ] **Step 1: Reemplazar el controller con filtro por rol**

Reemplazar el contenido completo de `backend/src/controllers/historiasClinicasController.js`:

```js
const HistoriaClinica = require('../models/historiaClinica');

// Campos que asistente_medico puede modificar (secciones 6 y 8)
const CAMPOS_ASISTENTE_MEDICO = [
  // Sección 6 — Motivo de consulta
  'mc_motivo_texto', 'mc_especifique',
  'mc_envejecimiento', 'mc_manchas', 'mc_flacidez_facial', 'mc_perdida_volumen',
  'mc_acne', 'mc_cicatrices_acne', 'mc_poros', 'mc_deshidratacion', 'mc_textura',
  'mc_grasa_localizada', 'mc_celulitis', 'mc_estrias', 'mc_flacidez', 'mc_adiposidad',
  'mc_perdida_peso', 'mc_control_metabolico', 'mc_obesidad', 'mc_hiperpigmentacion',
  'mc_alopecia', 'mc_bienestar', 'mc_armonizacion', 'mc_depilacion', 'mc_mejora_piel',
  // Sección 8 — Exploración física
  'sv_ta', 'sv_fc', 'sv_fr', 'sv_temperatura', 'sv_saturacion',
  'sv_peso', 'sv_talla', 'sv_imc',
  'habitus_exterior', 'fitzpatrick', 'glogau',
  'tipo_piel', 'tipo_rostro', 'lesiones_derm', 'tipo_lesion',
  'lesiones_descripcion', 'observaciones_generales',
  'med_cintura', 'med_cadera', 'med_muslo', 'med_brazo',
];

exports.get = async (req, res, next) => {
  try {
    const historia = await HistoriaClinica.findByPaciente(req.params.pacienteId);
    if (!historia) return res.status(404).json({ error: 'Historia clínica no encontrada' });
    res.json(historia);
  } catch (err) { next(err); }
};

exports.save = async (req, res, next) => {
  try {
    let body = req.body;
    if (req.user.rol === 'asistente_medico') {
      body = Object.fromEntries(
        Object.entries(req.body).filter(([k]) => CAMPOS_ASISTENTE_MEDICO.includes(k))
      );
    }
    const historia = await HistoriaClinica.upsert(req.params.pacienteId, body, req.user.id);
    res.json(historia);
  } catch (err) { next(err); }
};
```

- [ ] **Step 2: Probar que el backend inicia sin errores**

```bash
cd ~/elys/backend && node -e "require('./src/controllers/historiasClinicasController'); console.log('OK');"
```

Esperado: `OK`

- [ ] **Step 3: Commit**

```bash
git add backend/src/controllers/historiasClinicasController.js
git commit -m "feat: filtro de campos HC por rol (asistente_medico solo secciones 6 y 8)"
```

---

## Task 4: Backend — actualizar routes con requireRol + tipo de nota

**Files:**
- Modify: `backend/src/routes/historias-clinicas.js`
- Modify: `backend/src/routes/notas-visita.js`
- Modify: `backend/src/routes/pacientes.js`
- Modify: `backend/src/routes/citas.js`
- Modify: `backend/src/controllers/notasVisitaController.js`

**Context:** Aplicar `requireRol` en cada route. El controller de notas visita debe forzar el `tipo` según el rol del usuario: `asistente_medico` → `'medico'`, `cosmetista` → `'cosmetico'`, `admin` → usa el tipo enviado o `'medico'` por defecto. Los DELETE de notas visita solo los puede hacer `admin`.

- [ ] **Step 1: Actualizar `historias-clinicas.js`**

```js
// backend/src/routes/historias-clinicas.js
const router = require('express').Router();
const ctrl = require('../controllers/historiasClinicasController');
const { requireRol } = require('../middleware/roles');

router.get('/:pacienteId', ctrl.get);
router.put('/:pacienteId', requireRol('admin', 'asistente_medico'), ctrl.save);

module.exports = router;
```

- [ ] **Step 2: Actualizar `notasVisitaController.js` para forzar tipo por rol**

Reemplazar el contenido completo:

```js
// backend/src/controllers/notasVisitaController.js
const NotaVisita = require('../models/notaVisita');

function tipoSegunRol(rol, tipoEnviado) {
  if (rol === 'cosmetista') return 'cosmetico';
  if (rol === 'asistente_medico') return 'medico';
  return tipoEnviado || 'medico';
}

exports.getByCita = async (req, res, next) => {
  try {
    const nota = await NotaVisita.findByCita(req.query.cita_id);
    res.json(nota || null);
  } catch (err) { next(err); }
};

exports.getByPaciente = async (req, res, next) => {
  try {
    const notas = await NotaVisita.findByPaciente(req.params.pacienteId);
    // cosmetista solo ve sus notas tipo cosmetico
    if (req.user.rol === 'cosmetista') {
      return res.json(notas.filter(n => n.tipo === 'cosmetico'));
    }
    res.json(notas);
  } catch (err) { next(err); }
};

exports.create = async (req, res, next) => {
  try {
    const { cita_id, paciente_id } = req.body;
    if (!cita_id || !paciente_id)
      return res.status(400).json({ error: 'cita_id y paciente_id son requeridos' });
    const tipo = tipoSegunRol(req.user.rol, req.body.tipo);
    const nota = await NotaVisita.create({
      ...req.body, tipo, created_by: req.user.id,
    });
    res.status(201).json(nota);
  } catch (err) { next(err); }
};

exports.update = async (req, res, next) => {
  try {
    const nota = await NotaVisita.update(req.params.id, req.body);
    res.json(nota);
  } catch (err) { next(err); }
};

exports.remove = async (req, res, next) => {
  try {
    await NotaVisita.remove(req.params.id);
    res.json({ ok: true });
  } catch (err) { next(err); }
};
```

- [ ] **Step 3: Actualizar `notas-visita.js` route**

```js
// backend/src/routes/notas-visita.js
const router = require('express').Router();
const ctrl = require('../controllers/notasVisitaController');
const { requireRol } = require('../middleware/roles');

const puedeEscribir = requireRol('admin', 'asistente_medico', 'cosmetista');

router.get('/paciente/:pacienteId', ctrl.getByPaciente);
router.get('/', ctrl.getByCita);
router.post('/', puedeEscribir, ctrl.create);
router.put('/:id', puedeEscribir, ctrl.update);
router.delete('/:id', requireRol('admin'), ctrl.remove);

module.exports = router;
```

- [ ] **Step 4: Actualizar `pacientes.js` — restricción de PUT para asistente_general**

Reemplazar el route de PUT:

```js
// backend/src/routes/pacientes.js — fragmento a cambiar
// Línea actual:
// router.put('/:id', ctrl.update);
// Reemplazar con:
router.put('/:id', require('../middleware/roles').requireRol('admin', 'asistente_general'), ctrl.update);
```

El archivo completo resultante:
```js
// backend/src/routes/pacientes.js
const router = require('express').Router();
const ctrl = require('../controllers/pacientesController');
const upload = require('../middleware/upload');
const { requireRol } = require('../middleware/roles');

router.get('/buscar', ctrl.buscar);
router.get('/', ctrl.list);
router.post('/', requireRol('admin'), ctrl.create);
router.get('/:id', ctrl.getOne);
router.put('/:id', requireRol('admin', 'asistente_general'), ctrl.update);
router.post('/:id/foto', upload.single('foto'), ctrl.uploadFoto);
router.delete('/:id', requireRol('admin'), ctrl.remove);

module.exports = router;
```

- [ ] **Step 5: Actualizar `citas.js` — DELETE solo admin**

Leer el archivo actual con `cat backend/src/routes/citas.js` y agregar `requireRol('admin')` al DELETE. Ejemplo del resultado:

```js
// backend/src/routes/citas.js
const router = require('express').Router();
const ctrl = require('../controllers/citasController');
const { requireRol } = require('../middleware/roles');

router.get('/', ctrl.list);
router.post('/', ctrl.create);
router.put('/:id', ctrl.update);
router.delete('/:id', requireRol('admin'), ctrl.remove);

module.exports = router;
```

> **Nota:** Leer el archivo real antes de editar para no perder rutas existentes.

- [ ] **Step 6: Agregar método `remove` al modelo NotaVisita**

En `backend/src/models/notaVisita.js`, agregar al objeto `NotaVisita`:

```js
  async remove(id) {
    await pool.query('DELETE FROM notas_visita WHERE id = $1', [id]);
  },
```

También actualizar `findByCita` y `findByPaciente` para incluir el campo `tipo`:

En `findByCita`: agregar `nv.tipo` al SELECT.
En `findByPaciente`: el `SELECT nv.*` ya incluye todos los campos incluyendo `tipo`.

- [ ] **Step 7: Probar los endpoints con curl (backend debe estar corriendo)**

```bash
# Login como admin
TOKEN=$(curl -s -X POST http://localhost:3008/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@elys.com","password":"Admin123!"}' | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).token))")

# Probar que DELETE notas devuelve 404 (no hay nota con id 999, pero no debe dar 403)
curl -s -o /dev/null -w "%{http_code}" -X DELETE http://localhost:3008/api/notas-visita/999 \
  -H "Authorization: Bearer $TOKEN"
# Esperado: 404 o 500 (no 403)

# Probar que PUT /historias-clinicas funciona con admin
curl -s -o /dev/null -w "%{http_code}" -X PUT http://localhost:3008/api/historias-clinicas/1 \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"sv_ta":"120/80"}'
# Esperado: 200
```

- [ ] **Step 8: Commit**

```bash
git add backend/src/routes/historias-clinicas.js backend/src/routes/notas-visita.js \
        backend/src/routes/pacientes.js backend/src/routes/citas.js \
        backend/src/controllers/notasVisitaController.js \
        backend/src/models/notaVisita.js
git commit -m "feat: aplicar requireRol en routes, tipo de nota por rol, DELETE notas solo admin"
```

---

## Task 5: Backend — API de gestión de usuarios (admin panel)

**Files:**
- Create: `backend/src/controllers/usuariosController.js`
- Create: `backend/src/routes/usuarios.js`
- Modify: `backend/src/index.js`

**Context:** El admin necesita crear y editar usuarios con los nuevos roles. Solo se exponen los endpoints de list, create y update — no delete. El rol `admin` no puede asignarse desde la API (solo existe la Dra. Giovanna). Las contraseñas se hashean con bcryptjs (ya instalado, lo usa `auth.js`).

- [ ] **Step 1: Crear el controller**

```js
// backend/src/controllers/usuariosController.js
const pool = require('../db/pool');
const bcrypt = require('bcryptjs');

const ROLES_PERMITIDOS = ['asistente_medico', 'cosmetista', 'asistente_general'];

exports.list = async (req, res, next) => {
  try {
    const { rows } = await pool.query(
      `SELECT id, nombre, email, rol, cedula_profesional, created_at
       FROM usuarios ORDER BY nombre`
    );
    res.json(rows);
  } catch (err) { next(err); }
};

exports.create = async (req, res, next) => {
  try {
    const { nombre, email, password, rol, cedula_profesional } = req.body;
    if (!nombre || !email || !password || !rol)
      return res.status(400).json({ error: 'nombre, email, password y rol son requeridos' });
    if (!ROLES_PERMITIDOS.includes(rol))
      return res.status(400).json({ error: `rol debe ser uno de: ${ROLES_PERMITIDOS.join(', ')}` });

    const hash = await bcrypt.hash(password, 10);
    const { rows } = await pool.query(
      `INSERT INTO usuarios (nombre, email, password, rol, cedula_profesional)
       VALUES ($1, $2, $3, $4, $5)
       RETURNING id, nombre, email, rol, cedula_profesional, created_at`,
      [nombre, email, hash, rol, cedula_profesional || null]
    );
    res.status(201).json(rows[0]);
  } catch (err) {
    if (err.code === '23505') return res.status(409).json({ error: 'El email ya está registrado' });
    next(err);
  }
};

exports.update = async (req, res, next) => {
  try {
    const { nombre, email, password, rol, cedula_profesional } = req.body;
    if (rol && !ROLES_PERMITIDOS.includes(rol))
      return res.status(400).json({ error: `rol debe ser uno de: ${ROLES_PERMITIDOS.join(', ')}` });

    const setClauses = [];
    const values = [];
    let i = 1;

    if (nombre)            { setClauses.push(`nombre = $${i++}`);            values.push(nombre); }
    if (email)             { setClauses.push(`email = $${i++}`);             values.push(email); }
    if (rol)               { setClauses.push(`rol = $${i++}`);               values.push(rol); }
    if (cedula_profesional !== undefined) { setClauses.push(`cedula_profesional = $${i++}`); values.push(cedula_profesional || null); }
    if (password) {
      const hash = await bcrypt.hash(password, 10);
      setClauses.push(`password = $${i++}`);
      values.push(hash);
    }
    if (setClauses.length === 0)
      return res.status(400).json({ error: 'Nada que actualizar' });

    values.push(req.params.id);
    const { rows } = await pool.query(
      `UPDATE usuarios SET ${setClauses.join(', ')}
       WHERE id = $${i} AND rol != 'admin'
       RETURNING id, nombre, email, rol, cedula_profesional, created_at`,
      values
    );
    if (!rows[0]) return res.status(404).json({ error: 'Usuario no encontrado o es admin' });
    res.json(rows[0]);
  } catch (err) {
    if (err.code === '23505') return res.status(409).json({ error: 'El email ya está registrado' });
    next(err);
  }
};
```

- [ ] **Step 2: Crear el route**

```js
// backend/src/routes/usuarios.js
const router = require('express').Router();
const ctrl = require('../controllers/usuariosController');
const { requireRol } = require('../middleware/roles');

router.get('/',    requireRol('admin'), ctrl.list);
router.post('/',   requireRol('admin'), ctrl.create);
router.put('/:id', requireRol('admin'), ctrl.update);

module.exports = router;
```

- [ ] **Step 3: Registrar el route en `index.js`**

En `backend/src/index.js`, agregar después de la línea de consentimientos:

```js
app.use('/api/usuarios', authMiddleware, require('./routes/usuarios'));
```

- [ ] **Step 4: Probar la API**

```bash
# Backend corriendo. Obtener token admin
TOKEN=$(curl -s -X POST http://localhost:3008/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@elys.com","password":"Admin123!"}' | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).token))")

# Listar usuarios
curl -s http://localhost:3008/api/usuarios -H "Authorization: Bearer $TOKEN" | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).map(u=>u.email+' '+u.rol).join('\n')))"
# Esperado: lista con admin@elys.com admin

# Crear usuario de prueba
curl -s -X POST http://localhost:3008/api/usuarios \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"nombre":"Test Asistente","email":"test@elys.com","password":"Test123!","rol":"asistente_medico"}' \
  | node -e "process.stdin.on('data',d=>{ const u=JSON.parse(d); console.log(u.id?'CREADO OK':'ERROR', u.error||''); })"
# Esperado: CREADO OK

# Verificar que no se puede crear con rol admin
curl -s -X POST http://localhost:3008/api/usuarios \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"nombre":"Hack","email":"hack@elys.com","password":"x","rol":"admin"}' \
  | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).error))"
# Esperado: "rol debe ser uno de: asistente_medico, cosmetista, asistente_general"
```

- [ ] **Step 5: Limpiar usuario de prueba**

```bash
cd ~/elys && docker compose exec db psql -U elys_user -d elys -c "DELETE FROM usuarios WHERE email='test@elys.com';"
```

- [ ] **Step 6: Commit**

```bash
git add backend/src/controllers/usuariosController.js backend/src/routes/usuarios.js \
        backend/src/index.js
git commit -m "feat: API gestión de usuarios (/api/usuarios) solo admin"
```

---

## Task 6: Frontend — HistoriaClinicaForm prop editableSections

**Files:**
- Modify: `frontend/src/components/HistoriaClinicaForm.jsx`

**Context:** El componente `Section` (línea 109) recibe `{ title, open, onToggle, children }`. Se le agrega prop `disabled` que envuelve el contenido en `<fieldset disabled>` para deshabilitar nativamente todos los inputs. El form recibe `editableSections` (array de números de sección o `null` para todo editable). Si `editableSections` está definido, el botón "Guardar" solo envía los campos de esas secciones.

Los campos por sección que `asistente_medico` puede editar (secciones 6 y 8) son los mismos que en `CAMPOS_ASISTENTE_MEDICO` del backend.

- [ ] **Step 1: Modificar el componente `Section` (línea 109)**

Reemplazar:
```js
function Section({ title, open, onToggle, children }) {
  return (
```

Con:
```js
function Section({ title, open, onToggle, children, disabled = false }) {
  return (
```

Y en el JSX interno del Section (donde se renderiza `children`), envolver con fieldset cuando `disabled`:

Encontrar la parte que renderiza children (después del toggle button) y cambiar de:
```jsx
        <div className="p-4">{children}</div>
```
A:
```jsx
        <fieldset disabled={disabled} className={disabled ? 'opacity-60 pointer-events-none' : ''}>
          <div className="p-4">{children}</div>
        </fieldset>
```

> **Nota:** Leer el componente Section real (líneas 109-122) para ver la estructura exacta antes de editar.

- [ ] **Step 2: Actualizar la firma del componente principal y lógica de submit**

En `export default function HistoriaClinicaForm` (línea 244), cambiar la firma y el handleSubmit:

```js
// Campos permitidos por sección (para filtrar al guardar)
const CAMPOS_POR_SECCION = {
  6: [
    'mc_motivo_texto', 'mc_especifique',
    'mc_envejecimiento', 'mc_manchas', 'mc_flacidez_facial', 'mc_perdida_volumen',
    'mc_acne', 'mc_cicatrices_acne', 'mc_poros', 'mc_deshidratacion', 'mc_textura',
    'mc_grasa_localizada', 'mc_celulitis', 'mc_estrias', 'mc_flacidez', 'mc_adiposidad',
    'mc_perdida_peso', 'mc_control_metabolico', 'mc_obesidad', 'mc_hiperpigmentacion',
    'mc_alopecia', 'mc_bienestar', 'mc_armonizacion', 'mc_depilacion', 'mc_mejora_piel',
  ],
  8: [
    'sv_ta', 'sv_fc', 'sv_fr', 'sv_temperatura', 'sv_saturacion',
    'sv_peso', 'sv_talla', 'sv_imc',
    'habitus_exterior', 'fitzpatrick', 'glogau',
    'tipo_piel', 'tipo_rostro', 'lesiones_derm', 'tipo_lesion',
    'lesiones_descripcion', 'observaciones_generales',
    'med_cintura', 'med_cadera', 'med_muslo', 'med_brazo',
  ],
};

export default function HistoriaClinicaForm({ pacienteId, historia: initial, onSaved, editableSections = null }) {
```

Y cambiar `handleSubmit` (línea 271):

```js
  async function handleSubmit(e) {
    e.preventDefault();
    setLoading(true); setError(''); setOk(false);
    try {
      let body = { ...h, medicamentos_actuales: meds };
      if (editableSections !== null) {
        const allowedFields = editableSections.flatMap(s => CAMPOS_POR_SECCION[s] || []);
        body = Object.fromEntries(
          Object.entries(body).filter(([k]) => allowedFields.includes(k))
        );
      }
      const saved = await saveHistoria(pacienteId, body);
      setH(prev => ({ ...prev, ...saved }));
      setOk(true);
      if (onSaved) onSaved(saved);
    } catch (err) {
      setError(err.response?.data?.error || 'Error al guardar');
    } finally { setLoading(false); }
  }
```

- [ ] **Step 3: Pasar `disabled` a cada `<Section>` según `editableSections`**

Agregar `const canEdit = (n) => editableSections === null || editableSections.includes(n);` después de `const toggle = ...`.

Luego en cada `<Section ...>` agregar `disabled={!canEdit(N)}` donde N es el número de sección:

```jsx
<Section title="HC-01 · 1. ..." open={!!open[1]} onToggle={() => toggle(1)} disabled={!canEdit(1)}>
<Section title="HC-01 · 2. ..." open={!!open[2]} onToggle={() => toggle(2)} disabled={!canEdit(2)}>
<Section title="HC-01 · 3. ..." open={!!open[3]} onToggle={() => toggle(3)} disabled={!canEdit(3)}>
<Section title="HC-01 · 4. ..." open={!!open[4]} onToggle={() => toggle(4)} disabled={!canEdit(4)}>
<Section title="HC-01 · 5. ..." open={!!open[5]} onToggle={() => toggle(5)} disabled={!canEdit(5)}>
<Section title="HC-02 · 6. ..." open={!!open[6]} onToggle={() => toggle(6)} disabled={!canEdit(6)}>
<Section title="HC-02 · 7. ..." open={!!open[7]} onToggle={() => {...; toggle(7)}} disabled={!canEdit(7)}>
<Section title="HC-02 · 8. ..." open={!!open[8]} onToggle={() => toggle(8)} disabled={!canEdit(8)}>
```

- [ ] **Step 4: Verificar que el build pasa sin errores**

```bash
cd ~/elys/frontend && npm run build 2>&1 | grep -E "error|Error|✓"
```

Esperado: `✓ built in ...`

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/HistoriaClinicaForm.jsx
git commit -m "feat: HistoriaClinicaForm prop editableSections para restricción de secciones por rol"
```

---

## Task 7: Frontend — NotaVisitaModal tipo cosmético

**Files:**
- Modify: `frontend/src/components/NotaVisitaModal.jsx`

**Context:** El modal actual muestra todos los campos SOAP (evolución, diagnóstico, pronóstico, tratamiento/indicaciones, signos vitales). Para `cosmetista`, solo debe mostrar un campo de texto "Nota de tratamiento cosmético". El modal recibe `rol` como prop, o puede leerlo de `localStorage.getItem('rol')`.

- [ ] **Step 1: Modificar NotaVisitaModal para soportar tipo cosmético**

Leer el archivo completo (es ~120 líneas). Luego hacer estos cambios:

**Al inicio del componente**, agregar lectura de rol:
```js
export default function NotaVisitaModal({ cita, pacienteId, nota, onClose, onSaved }) {
  const rol = localStorage.getItem('rol');
  const esCosmética = rol === 'cosmetista';
```

**En el estado inicial y useEffect**, mantener igual pero la cosmetista solo usa `evolucion` (lo usamos como campo de nota cosmética).

**En el JSX del form**, reemplazar el bloque de campos con condicional:

```jsx
{esCosmética ? (
  <div>
    <label className="block text-sm font-medium mb-1" style={{ color: 'var(--color-dark)' }}>
      Nota del tratamiento cosmético
    </label>
    <textarea
      value={form.evolucion}
      onChange={e => set('evolucion', e.target.value)}
      rows={5}
      placeholder="Describe el tratamiento realizado, productos usados y observaciones..."
      className="w-full border rounded-lg px-3 py-2 text-sm resize-none"
      style={{ borderColor: 'var(--color-sage)' }}
    />
  </div>
) : (
  /* Los campos SOAP existentes van aquí sin cambios */
  <>
    {[
      ['evolucion', 'Evolución clínica', 4],
      ['diagnostico', 'Diagnóstico', 2],
      ... (resto de campos existentes)
    ]}
  </>
)}
```

**Al enviar** (`handleSubmit`), agregar el `tipo` al payload:

```js
const tipo = esCosmética ? 'cosmetico' : 'medico';
const saved = nota
  ? await updateNota(nota.id, form)
  : await createNota({ ...form, tipo, cita_id: cita.id, paciente_id: pacienteId });
```

**El título del modal** cambia según rol:
```jsx
<h2 ...>{esCosmética ? 'Nota de Tratamiento Cosmético' : 'Nota de Evolución'}</h2>
```

- [ ] **Step 2: Verificar build**

```bash
cd ~/elys/frontend && npm run build 2>&1 | grep -E "error|Error|✓"
```

Esperado: `✓ built in ...`

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/NotaVisitaModal.jsx
git commit -m "feat: NotaVisitaModal tipo cosmético para rol cosmetista"
```

---

## Task 8: Frontend — PacienteDetallePage + App.jsx controles por rol

**Files:**
- Modify: `frontend/src/pages/PacienteDetallePage.jsx`
- Modify: `frontend/src/App.jsx`

**Context:**
- `PacienteDetallePage`: el botón "Editar datos" (línea 125) solo debe ser visible para `admin` y `asistente_general`. El tab "Notas de Visita" es visible para `admin`, `asistente_medico`, `cosmetista`. El `HistoriaClinicaForm` recibe `editableSections={[6,8]}` cuando el rol es `asistente_medico`.
- `App.jsx`: nav items Empleados y Finanzas solo para `admin`. Tratamientos solo para `admin`, `asistente_medico`, `cosmetista`.

- [ ] **Step 1: Actualizar PacienteDetallePage**

Al inicio del componente, agregar:
```js
const rol = localStorage.getItem('rol');
```

Botón "Editar datos" (línea ~125): agregar condicional:
```jsx
{(rol === 'admin' || rol === 'asistente_general') && (
  <button onClick={() => setEditModal(true)} ...>
    Editar datos
  </button>
)}
```

Pasar `editableSections` al `HistoriaClinicaForm`:
```jsx
{tab === 'historia' && (
  historia
    ? <HistoriaClinicaForm
        pacienteId={id}
        historia={historia}
        onSaved={h => setHistoria(h)}
        editableSections={rol === 'asistente_medico' ? [6, 8] : null}
      />
    : <p ...>No se encontró la historia clínica...</p>
)}
```

Tab "Notas de Visita" — ocultar para `asistente_general`:
```jsx
{rol !== 'asistente_general' && (
  <TabBtn active={tab === 'notas'} onClick={() => setTab('notas')}>
    Notas de Visita {notas.length ? `(${notas.length})` : ''}
  </TabBtn>
)}
```

Tab "Consentimientos" — ocultar para `asistente_general` y `cosmetista`:
```jsx
{(rol === 'admin' || rol === 'asistente_medico') && (
  <TabBtn active={tab === 'consentimientos'} onClick={() => setTab('consentimientos')}>
    Consentimientos
  </TabBtn>
)}
```

En el tab de notas (línea ~188), el botón de eliminar nota solo visible para `admin`:
```jsx
{rol === 'admin' && (
  <button onClick={() => handleDeleteNota(n.id)} ...>Eliminar</button>
)}
```

> **Nota:** leer el archivo para encontrar las líneas exactas del botón de eliminar nota, si existe. Si no existe aún, no agregar.

- [ ] **Step 2: Actualizar App.jsx nav items**

En `App.jsx`, en la sección de nav (alrededor de línea 105):

```jsx
{/* Tratamientos: admin, asistente_medico, cosmetista */}
{(rol === 'admin' || rol === 'asistente_medico' || rol === 'cosmetista') &&
  <NavItem to="/tratamientos">Tratamientos</NavItem>}

{/* Finanzas: solo admin */}
{rol === 'admin' && <NavItem to="/finanzas">Finanzas</NavItem>}

{/* Empleados: solo admin */}
{rol === 'admin' && <NavItem to="/empleados">Empleados</NavItem>}
```

Y en Routes (alrededor de línea 134):
```jsx
{(rol === 'admin' || rol === 'asistente_medico' || rol === 'cosmetista') &&
  <Route path="/tratamientos" element={<TratamientosPage />} />}
{rol === 'admin' && <Route path="/finanzas" element={<FinanzasPage />} />}
{rol === 'admin' && <Route path="/empleados" element={<EmpleadosPage />} />}
{rol === 'admin' && <Route path="/empleados/nuevo" element={<EmpleadoFormPage />} />}
{rol === 'admin' && <Route path="/empleados/:id" element={<EmpleadoDetallePage />} />}
{rol === 'admin' && <Route path="/empleados/:id/editar" element={<EmpleadoFormPage />} />}
```

> **Nota:** Leer App.jsx completo antes de editar para ajustar las líneas exactas.

- [ ] **Step 3: Verificar build**

```bash
cd ~/elys/frontend && npm run build 2>&1 | grep -E "error|Error|✓"
```

Esperado: `✓ built in ...`

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/PacienteDetallePage.jsx frontend/src/App.jsx
git commit -m "feat: controles de UI por rol en PacienteDetallePage y App nav"
```

---

## Task 9: Frontend — Panel de gestión de usuarios en EmpleadosPage

**Files:**
- Create: `frontend/src/api/usuarios.js`
- Create: `frontend/src/components/usuarios/UsuarioModal.jsx`
- Modify: `frontend/src/pages/EmpleadosPage.jsx`

**Context:** El admin ve una sección "Usuarios del sistema" al final de `EmpleadosPage`. Muestra una tabla con nombre, email, rol (chip de color) y cédula profesional. Botones: "Nuevo usuario" y editar por fila. No hay eliminación de usuarios. El rol `admin` no aparece en el select del modal (solo los 3 roles asignables).

- [ ] **Step 1: Crear `frontend/src/api/usuarios.js`**

```js
// frontend/src/api/usuarios.js
import api from './client';

export const getUsuarios  = ()        => api.get('/usuarios').then(r => r.data);
export const createUsuario = (data)   => api.post('/usuarios', data).then(r => r.data);
export const updateUsuario = (id, d)  => api.put(`/usuarios/${id}`, d).then(r => r.data);
```

- [ ] **Step 2: Crear `frontend/src/components/usuarios/UsuarioModal.jsx`**

```jsx
// frontend/src/components/usuarios/UsuarioModal.jsx
import { useState, useEffect } from 'react';
import { X } from 'lucide-react';
import { createUsuario, updateUsuario } from '../../api/usuarios';

const ROL_LABELS = {
  asistente_medico:  'Asistente Médico',
  cosmetista:        'Cosmetista',
  asistente_general: 'Asistente General',
};

export default function UsuarioModal({ usuario, onSave, onClose }) {
  const esNuevo = !usuario;
  const [form, setForm] = useState({
    nombre: '', email: '', password: '', rol: 'asistente_medico', cedula_profesional: '',
  });
  const [loading, setLoading] = useState(false);
  const [error, setError]     = useState('');

  useEffect(() => {
    if (usuario) setForm({
      nombre: usuario.nombre || '',
      email: usuario.email || '',
      password: '',
      rol: usuario.rol || 'asistente_medico',
      cedula_profesional: usuario.cedula_profesional || '',
    });
  }, [usuario]);

  const set = (k, v) => setForm(f => ({ ...f, [k]: v }));

  async function handleSubmit(e) {
    e.preventDefault();
    setLoading(true); setError('');
    try {
      const payload = { ...form };
      if (!payload.password) delete payload.password;
      const saved = esNuevo
        ? await createUsuario(payload)
        : await updateUsuario(usuario.id, payload);
      onSave(saved);
    } catch (err) {
      setError(err.response?.data?.error || 'Error al guardar');
    } finally { setLoading(false); }
  }

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4">
      <div className="bg-white rounded-2xl shadow-xl w-full max-w-md"
           style={{ borderTop: '4px solid var(--color-accent)' }}>
        <div className="p-6">
          <div className="flex items-center justify-between mb-4">
            <h2 className="text-base font-bold" style={{ color: 'var(--color-dark)' }}>
              {esNuevo ? 'Nuevo usuario' : 'Editar usuario'}
            </h2>
            <button onClick={onClose}><X className="w-5 h-5" style={{ color: 'var(--color-accent)' }} /></button>
          </div>

          {error && <p className="text-red-500 text-sm mb-3">{error}</p>}

          <form onSubmit={handleSubmit} className="space-y-3">
            {[['nombre','Nombre completo','text'],['email','Email','email']].map(([k, label, type]) => (
              <div key={k}>
                <label className="block text-xs font-medium mb-1" style={{ color: 'var(--color-dark)' }}>{label}</label>
                <input type={type} value={form[k]} onChange={e => set(k, e.target.value)} required
                       className="w-full border rounded-lg px-3 py-2 text-sm"
                       style={{ borderColor: 'var(--color-sage)' }} />
              </div>
            ))}

            <div>
              <label className="block text-xs font-medium mb-1" style={{ color: 'var(--color-dark)' }}>
                {esNuevo ? 'Contraseña' : 'Nueva contraseña (dejar vacío para no cambiar)'}
              </label>
              <input type="password" value={form.password} onChange={e => set('password', e.target.value)}
                     required={esNuevo} minLength={6}
                     className="w-full border rounded-lg px-3 py-2 text-sm"
                     style={{ borderColor: 'var(--color-sage)' }} />
            </div>

            <div>
              <label className="block text-xs font-medium mb-1" style={{ color: 'var(--color-dark)' }}>Rol</label>
              <select value={form.rol} onChange={e => set('rol', e.target.value)}
                      className="w-full border rounded-lg px-3 py-2 text-sm"
                      style={{ borderColor: 'var(--color-sage)' }}>
                {Object.entries(ROL_LABELS).map(([v, l]) => (
                  <option key={v} value={v}>{l}</option>
                ))}
              </select>
            </div>

            {(form.rol === 'asistente_medico') && (
              <div>
                <label className="block text-xs font-medium mb-1" style={{ color: 'var(--color-dark)' }}>
                  Cédula profesional
                </label>
                <input type="text" value={form.cedula_profesional}
                       onChange={e => set('cedula_profesional', e.target.value)}
                       className="w-full border rounded-lg px-3 py-2 text-sm"
                       style={{ borderColor: 'var(--color-sage)' }} />
              </div>
            )}

            <div className="flex gap-2 pt-2">
              <button type="button" onClick={onClose}
                      className="flex-1 py-2 text-sm border rounded-lg"
                      style={{ borderColor: 'var(--color-sage)', color: 'var(--color-accent)' }}>
                Cancelar
              </button>
              <button type="submit" disabled={loading}
                      className="flex-1 py-2 text-sm text-white rounded-lg font-medium disabled:opacity-50"
                      style={{ backgroundColor: 'var(--color-accent)' }}>
                {loading ? 'Guardando…' : 'Guardar'}
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Agregar sección de usuarios al final de `EmpleadosPage.jsx`**

Leer el archivo actual. Agregar al inicio del archivo:

```js
import { getUsuarios } from '../api/usuarios';
import UsuarioModal from '../components/usuarios/UsuarioModal';
```

Agregar estado para usuarios:
```js
const rol = localStorage.getItem('rol');
const [usuarios, setUsuarios]       = useState([]);
const [usuarioModal, setUsuarioModal] = useState(null); // { usuario?: obj } | null
```

Agregar carga de usuarios en el `useEffect` inicial (solo si es admin):
```js
if (rol === 'admin') {
  getUsuarios().then(setUsuarios).catch(console.error);
}
```

Al final del JSX del componente (antes del último `</div>` de cierre), agregar la sección:

```jsx
{rol === 'admin' && (
  <div className="mt-8">
    <div className="flex items-center justify-between mb-4">
      <h2 className="text-lg font-bold" style={{ color: 'var(--color-dark)' }}>
        Usuarios del sistema
      </h2>
      <button onClick={() => setUsuarioModal({})}
              className="px-4 py-2 text-sm text-white rounded-lg font-medium"
              style={{ backgroundColor: 'var(--color-accent)' }}>
        + Nuevo usuario
      </button>
    </div>
    <div className="bg-white rounded-xl border overflow-hidden"
         style={{ borderColor: 'var(--color-sage)' }}>
      <table className="w-full text-sm">
        <thead style={{ backgroundColor: 'var(--color-primary)' }}>
          <tr>
            {['Nombre','Email','Rol','Cédula',''].map(h => (
              <th key={h} className="text-left px-4 py-2 text-xs font-semibold"
                  style={{ color: 'var(--color-dark)' }}>{h}</th>
            ))}
          </tr>
        </thead>
        <tbody>
          {usuarios.map(u => (
            <tr key={u.id} className="border-t" style={{ borderColor: 'var(--color-sage)' }}>
              <td className="px-4 py-2 font-medium" style={{ color: 'var(--color-dark)' }}>{u.nombre}</td>
              <td className="px-4 py-2 text-gray-500">{u.email}</td>
              <td className="px-4 py-2">
                <span className="text-xs px-2 py-0.5 rounded-full text-white"
                      style={{ backgroundColor: {
                        admin: '#887482',
                        asistente_medico: '#4a7c6a',
                        cosmetista: '#aba3ba',
                        asistente_general: '#6b7280',
                      }[u.rol] || '#887482' }}>
                  {u.rol === 'admin' ? 'Admin' :
                   u.rol === 'asistente_medico' ? 'Asist. Médico' :
                   u.rol === 'cosmetista' ? 'Cosmetista' : 'Asist. General'}
                </span>
              </td>
              <td className="px-4 py-2 text-gray-500">{u.cedula_profesional || '—'}</td>
              <td className="px-4 py-2">
                {u.rol !== 'admin' && (
                  <button onClick={() => setUsuarioModal({ usuario: u })}
                          className="text-xs px-3 py-1 border rounded"
                          style={{ borderColor: 'var(--color-sage)', color: 'var(--color-accent)' }}>
                    Editar
                  </button>
                )}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>

    {usuarioModal && (
      <UsuarioModal
        usuario={usuarioModal.usuario}
        onSave={() => { setUsuarioModal(null); getUsuarios().then(setUsuarios); }}
        onClose={() => setUsuarioModal(null)}
      />
    )}
  </div>
)}
```

- [ ] **Step 4: Verificar build**

```bash
cd ~/elys/frontend && npm run build 2>&1 | grep -E "error|Error|✓"
```

Esperado: `✓ built in ...`

- [ ] **Step 5: Commit y deploy**

```bash
git add frontend/src/api/usuarios.js \
        frontend/src/components/usuarios/UsuarioModal.jsx \
        frontend/src/pages/EmpleadosPage.jsx
git commit -m "feat: panel gestión de usuarios en EmpleadosPage (admin)"

cd ~/elys && ./deploy.sh
```

---

## Verificación final

- [ ] Entrar como admin → nav muestra Empleados, Tratamientos, Finanzas
- [ ] Crear usuario con rol `asistente_medico` desde EmpleadosPage → panel Usuarios del sistema
- [ ] Login con ese usuario → nav muestra solo Citas, Pacientes, Tratamientos (sin Empleados, Finanzas)
- [ ] Abrir expediente clínico → secciones 1-5 y 7 aparecen sin botón editar (opacas); secciones 6 y 8 editables
- [ ] Crear usuario con rol `cosmetista` → al abrir nota de visita, aparece solo campo cosmético
- [ ] Crear usuario con rol `asistente_general` → solo ve botón "Editar datos" en ficha paciente, sin tab Notas ni HC editable
